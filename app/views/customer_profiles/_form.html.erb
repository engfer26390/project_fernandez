<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <div class="well">
        <h2>Actualizar Perfil</h2>

          <%= form_for(@customer_profile) do |f| %>
            <% if @customer_profile.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@customer_profile.errors.count, "error") %> prohibited this customer_profile from being saved:</h2>

                <ul>
                <% @customer_profile.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
                </ul>
              </div>
            <% end %>

            <div class="form-group">
              <div class="field">
                <%= f.label :availableHours %><br />
                <%= f.text_area :availableHours %>
              </div>
            </div>

            <div class="actions">
              <%= f.submit %>
            </div>
          <% end %>
      </div>
    </div>
  </div>
</div>

<%= form_for(@worker_profile) do |f| %>
  <% if @worker_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@worker_profile.errors.count, "error") %> prohibited this worker_profile from being saved:</h2>

      <ul>
      <% @worker_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
